name: Build sing-box testing (Android, Windows, Linux, Darwin)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release and GUI version (e.g. 1.15.0-alpha.2-reF1nd)'
        required: true
      core_base_version:
        description: 'Core base version used before the source commit suffix (e.g. 1.15.0-alpha.2)'
        required: true
      core_source:
        description: 'Optional core source: owner/repo [branch, tag, or commit]'
        required: false
        default: ''
      android_gui_source:
        description: 'Optional Android GUI source: owner/repo [branch, tag, or commit]'
        required: false
        default: ''
      desktop_gui_source:
        description: 'Optional desktop GUI source: owner/repo [branch, tag, or commit]'
        required: false
        default: ''
      build_cli:
        description: 'Build CLI'
        required: false
        type: boolean
        default: true
      build_android_gui:
        description: 'Build Android'
        required: false
        type: boolean
        default: true
      build_windows_gui:
        description: 'Build Windows GUI'
        required: false
        type: boolean
        default: true
      build_linux_gui:
        description: 'Build Linux GUI'
        required: false
        type: boolean
        default: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.version }}
  cancel-in-progress: true

env:
  GO_VERSION: '1.26.7'
  GO_VERSION_DARWIN: '1.26.7'

jobs:
  resolve_source:
    name: Resolve source repositories
    runs-on: ubuntu-latest
    outputs:
      source_sha: ${{ steps.source.outputs.source_sha }}
      core_version: ${{ steps.source.outputs.core_version }}
      core_repository: ${{ steps.sources.outputs.core_repository }}
      core_ref: ${{ steps.sources.outputs.core_ref }}
      android_gui_repository: ${{ steps.sources.outputs.android_gui_repository }}
      android_gui_ref: ${{ steps.sources.outputs.android_gui_ref }}
      android_gui_sha: ${{ steps.source.outputs.android_gui_sha }}
      desktop_gui_repository: ${{ steps.sources.outputs.desktop_gui_repository }}
      desktop_gui_ref: ${{ steps.sources.outputs.desktop_gui_ref }}
      desktop_gui_sha: ${{ steps.source.outputs.desktop_gui_sha }}
    steps:
      - name: Parse source selections
        id: sources
        shell: bash
        env:
          CORE_SOURCE: ${{ inputs.core_source }}
          ANDROID_GUI_SOURCE: ${{ inputs.android_gui_source }}
          DESKTOP_GUI_SOURCE: ${{ inputs.desktop_gui_source }}
        run: |
          set -euo pipefail
          parse_source() {
            local prefix="$1"
            local value="$2"
            local default_repository="$3"
            local default_ref="$4"
            local parts=()
            read -r -a parts <<< "$value" || true
            if (( ${#parts[@]} > 2 )); then
              echo "invalid ${prefix} source: expected 'owner/repo [ref]'" >&2
              exit 1
            fi
            local repository="${parts[0]:-$default_repository}"
            local ref="${parts[1]:-$default_ref}"
            if [[ ! "$repository" =~ ^[0-9A-Za-z_.-]+/[0-9A-Za-z_.-]+$ ]]; then
              echo "invalid ${prefix} repository: $repository" >&2
              exit 1
            fi
            if [[ ! "$ref" =~ ^[0-9A-Fa-f]{7,40}$ ]] && ! git check-ref-format --branch "$ref" >/dev/null 2>&1; then
              echo "invalid ${prefix} ref: $ref" >&2
              exit 1
            fi
            {
              echo "${prefix}_repository=$repository"
              echo "${prefix}_ref=$ref"
            } >> "$GITHUB_OUTPUT"
          }
          parse_source core "$CORE_SOURCE" 'reF1nd/sing-box' 'reF1nd-testing'
          parse_source android_gui "$ANDROID_GUI_SOURCE" 'reF1nd/sing-box-for-android' 'reF1nd-testing'
          parse_source desktop_gui "$DESKTOP_GUI_SOURCE" 'reF1nd/sing-box-for-desktop' 'reF1nd-dev'

      - name: Checkout sing-box
        uses: actions/checkout@v6
        with:
          repository: ${{ steps.sources.outputs.core_repository }}
          ref: ${{ steps.sources.outputs.core_ref }}
          path: sing-box
          fetch-depth: 1

      - name: Checkout Android GUI source
        uses: actions/checkout@v6
        with:
          repository: ${{ steps.sources.outputs.android_gui_repository }}
          ref: ${{ steps.sources.outputs.android_gui_ref }}
          path: android-gui-source
          fetch-depth: 1

      - name: Checkout desktop GUI source
        uses: actions/checkout@v6
        with:
          repository: ${{ steps.sources.outputs.desktop_gui_repository }}
          ref: ${{ steps.sources.outputs.desktop_gui_ref }}
          path: desktop-gui-source
          fetch-depth: 1

      - name: Resolve source commits and core version
        id: source
        run: |
          set -euo pipefail
          if [[ ! "$RELEASE_VERSION" =~ ^[0-9A-Za-z][0-9A-Za-z.+-]*$ ]]; then
            echo "invalid release version: $RELEASE_VERSION" >&2
            exit 1
          fi
          if [[ ! "$CORE_BASE_VERSION" =~ ^[0-9A-Za-z][0-9A-Za-z.+-]*$ ]]; then
            echo "invalid core base version: $CORE_BASE_VERSION" >&2
            exit 1
          fi
          source_sha=$(git -C sing-box rev-parse HEAD)
          android_gui_sha=$(git -C android-gui-source rev-parse HEAD)
          desktop_gui_sha=$(git -C desktop-gui-source rev-parse HEAD)
          core_version="${CORE_BASE_VERSION}-${source_sha:0:9}"
          {
            echo "source_sha=${source_sha}"
            echo "android_gui_sha=${android_gui_sha}"
            echo "desktop_gui_sha=${desktop_gui_sha}"
            echo "core_version=${core_version}"
          } >> "$GITHUB_OUTPUT"
          echo "Resolved core at ${source_sha}"
          echo "Resolved Android GUI at ${android_gui_sha}"
          echo "Resolved desktop GUI at ${desktop_gui_sha}"
          echo "Core version: ${core_version}"
        env:
          RELEASE_VERSION: ${{ inputs.version }}
          CORE_BASE_VERSION: ${{ inputs.core_base_version }}

  build_cli:
    if: ${{ github.event.inputs.build_cli == 'true' }}
    name: Build CLI ${{ matrix.platform }}-${{ matrix.arch }} ${{ matrix.variant || matrix.legacy_name }}
    runs-on: ${{ matrix.os }}
    needs: resolve_source
    strategy:
      fail-fast: false
      max-parallel: 13
      matrix:
        include:
          - platform: linux
            arch: arm64
            os: ubuntu-26.04
            goos: linux
            goarch: arm64
            goamd64: ''
            binary: sing-box
            variant: purego
            artifact_name: binary-linux-arm64-purego
            naive: true
          - platform: linux
            arch: arm64
            os: ubuntu-26.04
            goos: linux
            goarch: arm64
            goamd64: ''
            binary: sing-box
            variant: glibc
            artifact_name: binary-linux-arm64-glibc
            cgo: '1'
            naive: true
          - platform: linux
            arch: arm64
            os: ubuntu-26.04
            goos: linux
            goarch: arm64
            goamd64: ''
            binary: sing-box
            variant: musl
            artifact_name: binary-linux-arm64-musl
            cgo: '1'
            naive: true
          - platform: linux
            arch: amd64
            os: ubuntu-26.04
            goos: linux
            goarch: amd64
            goamd64: v1
            binary: sing-box
            variant: purego
            artifact_name: binary-linux-amd64-purego
            naive: true
          - platform: linux
            arch: amd64
            os: ubuntu-26.04
            goos: linux
            goarch: amd64
            goamd64: v1
            binary: sing-box
            variant: glibc
            artifact_name: binary-linux-amd64-glibc
            cgo: '1'
            naive: true
          - platform: linux
            arch: amd64
            os: ubuntu-26.04
            goos: linux
            goarch: amd64
            goamd64: v1
            binary: sing-box
            variant: musl
            artifact_name: binary-linux-amd64-musl
            cgo: '1'
            naive: true
          - platform: linux
            arch: amd64v3
            os: ubuntu-26.04
            goos: linux
            goarch: amd64
            goamd64: v3
            binary: sing-box
            variant: purego
            artifact_name: binary-linux-amd64v3-purego
            naive: true
          - platform: linux
            arch: amd64v3
            os: ubuntu-26.04
            goos: linux
            goarch: amd64
            goamd64: v3
            binary: sing-box
            variant: glibc
            artifact_name: binary-linux-amd64v3-glibc
            cgo: '1'
            naive: true
          - platform: linux
            arch: amd64v3
            os: ubuntu-26.04
            goos: linux
            goarch: amd64
            goamd64: v3
            binary: sing-box
            variant: musl
            artifact_name: binary-linux-amd64v3-musl
            cgo: '1'
            naive: true
          - platform: windows
            arch: arm64
            os: windows-latest
            goos: windows
            goarch: arm64
            goamd64: ''
            binary: sing-box.exe
            variant: purego
            artifact_name: binary-windows-arm64
            naive: true
          - platform: windows
            arch: amd64
            os: windows-latest
            goos: windows
            goarch: amd64
            goamd64: v1
            binary: sing-box.exe
            variant: purego
            artifact_name: binary-windows-amd64
            naive: true
          - platform: windows
            arch: amd64v3
            os: windows-latest
            goos: windows
            goarch: amd64
            goamd64: v3
            binary: sing-box.exe
            variant: purego
            artifact_name: binary-windows-amd64v3
            naive: true
          - platform: darwin
            arch: arm64
            os: macos-latest
            goos: darwin
            goarch: arm64
            goamd64: ''
            binary: sing-box
            artifact_name: binary-darwin-arm64
            cgo: '1'
            naive: true
          - platform: darwin
            arch: amd64
            os: macos-latest
            goos: darwin
            goarch: amd64
            goamd64: v1
            binary: sing-box
            artifact_name: binary-darwin-amd64
            cgo: '1'
            naive: true
          - platform: darwin
            arch: amd64v3
            os: macos-latest
            goos: darwin
            goarch: amd64
            goamd64: v3
            binary: sing-box
            artifact_name: binary-darwin-amd64v3
            cgo: '1'
            naive: true
          - platform: darwin
            arch: amd64
            os: macos-latest
            goos: darwin
            goarch: amd64
            goamd64: v1
            binary: sing-box
            artifact_name: binary-darwin-amd64-legacy-macos-10.13
            legacy_macos: true
            legacy_name: macos-10.13
            macos_deployment_target: '10.13'
            cgo: '1'
            naive: false
          - platform: android
            arch: 386
            os: ubuntu-26.04
            goos: android
            goarch: 386
            goamd64: ''
            binary: sing-box
            artifact_name: binary-android-386
            cgo: '1'
            cc_triple: 'i686-linux-android23'
            naive: true
          - platform: android
            arch: amd64
            os: ubuntu-26.04
            goos: android
            goarch: amd64
            goamd64: v1
            binary: sing-box
            artifact_name: binary-android-amd64
            cgo: '1'
            cc_triple: 'x86_64-linux-android23'
            naive: true
          - platform: android
            arch: amd64v3
            os: ubuntu-26.04
            goos: android
            goarch: amd64
            goamd64: v3
            binary: sing-box
            artifact_name: binary-android-amd64v3
            cgo: '1'
            cc_triple: 'x86_64-linux-android23'
            naive: true
          - platform: android
            arch: arm
            os: ubuntu-26.04
            goos: android
            goarch: arm
            goamd64: ''
            binary: sing-box
            artifact_name: binary-android-arm
            cgo: '1'
            cc_triple: 'armv7a-linux-androideabi23'
            naive: true
          - platform: android
            arch: arm64
            os: ubuntu-26.04
            goos: android
            goarch: arm64
            goamd64: ''
            binary: sing-box
            artifact_name: binary-android-arm64
            cgo: '1'
            cc_triple: 'aarch64-linux-android23'
            naive: true
    steps:
      - name: Setup Go for Darwin
        if: matrix.platform == 'darwin' && matrix.legacy_macos != true
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION_DARWIN }}
          cache: false

      - name: Setup Go
        if: matrix.platform != 'darwin'
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Checkout sing-box
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.core_repository }}
          ref: ${{ needs.resolve_source.outputs.source_sha }}
          path: sing-box
          fetch-depth: 1

      - name: Set build tag
        shell: bash
        run: |
          set -xeuo pipefail
          cd sing-box
          TAG="v${{ needs.resolve_source.outputs.core_version }}"
          git tag -f "${TAG}" "${{ needs.resolve_source.outputs.source_sha }}"

      - name: Cache Go for macOS 10.13
        if: matrix.legacy_macos == true
        id: cache-go-for-macos1013
        uses: actions/cache@v5
        with:
          path: ~/go/go_osx
          key: go-osx-macos1013-${{ hashFiles('sing-box/.github/setup_go_for_macos1013.sh') }}

      - name: Setup Go for macOS 10.13
        if: matrix.legacy_macos == true && steps.cache-go-for-macos1013.outputs.cache-hit != 'true'
        run: sing-box/.github/setup_go_for_macos1013.sh
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Activate Go for macOS 10.13
        if: matrix.legacy_macos == true
        run: |
          echo "PATH=$HOME/go/go_osx/bin:$PATH" >> "$GITHUB_ENV"
          echo "GOROOT=$HOME/go/go_osx" >> "$GITHUB_ENV"

      - name: Cache Go modules for Darwin
        if: matrix.platform == 'darwin'
        uses: actions/cache@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-macos-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-mod-macos-

      - name: Restore Go modules for Linux and Android
        if: matrix.platform == 'linux' || matrix.platform == 'android'
        uses: actions/cache/restore@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-linux-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-mod-linux-

      - name: Cache Go modules for Windows
        if: matrix.platform == 'windows'
        uses: actions/cache@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-windows-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-mod-windows-

      - name: Setup Android NDK
        if: matrix.platform == 'android'
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r28
          #local-cache: true

      - name: Clone cronet-go
        if: matrix.variant == 'glibc' || matrix.variant == 'musl'
        shell: bash
        run: |
          set -xeuo pipefail
          CRONET_GO_VERSION=$(cat sing-box/.github/CRONET_GO_VERSION)
          git init ~/cronet-go
          git -C ~/cronet-go remote add origin https://github.com/sagernet/cronet-go.git
          git -C ~/cronet-go sparse-checkout set --no-cone '/*' '!/lib'
          git -C ~/cronet-go fetch --depth=1 --filter=blob:none origin "$CRONET_GO_VERSION"
          git -C ~/cronet-go checkout FETCH_HEAD
          git -C ~/cronet-go submodule update --init --recursive --depth=1

      - name: Regenerate Debian keyring
        if: matrix.variant == 'glibc' || matrix.variant == 'musl'
        shell: bash
        run: |
          set -xeuo pipefail
          rm -f ~/cronet-go/naiveproxy/src/build/linux/sysroot_scripts/keyring.gpg
          cd ~/cronet-go
          GPG_TTY=/dev/null ./naiveproxy/src/build/linux/sysroot_scripts/generate_keyring.sh

      - name: Download Chromium toolchain
        if: matrix.variant == 'glibc' || matrix.variant == 'musl'
        shell: bash
        run: |
          set -xeuo pipefail
          cd ~/cronet-go
          if [[ "${{ matrix.variant }}" == "musl" ]]; then
            go run ./cmd/build-naive --target=linux/${{ matrix.goarch }} --libc=musl download-toolchain
          else
            go run ./cmd/build-naive --target=linux/${{ matrix.goarch }} download-toolchain
          fi

      - name: Set Chromium toolchain environment
        if: matrix.variant == 'glibc' || matrix.variant == 'musl'
        shell: bash
        run: |
          set -xeuo pipefail
          cd ~/cronet-go
          if [[ "${{ matrix.variant }}" == "musl" ]]; then
            go run ./cmd/build-naive --target=linux/${{ matrix.goarch }} --libc=musl env >> $GITHUB_ENV
          else
            go run ./cmd/build-naive --target=linux/${{ matrix.goarch }} env >> $GITHUB_ENV
          fi

      - name: Compute build environment
        id: env
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ matrix.platform }}" == "windows" ]]; then
            TAGS=$(cat sing-box/release/DEFAULT_BUILD_TAGS_WINDOWS)
          elif [[ "${{ matrix.naive }}" == "true" ]]; then
            TAGS=$(cat sing-box/release/DEFAULT_BUILD_TAGS)
          else
            TAGS=$(cat sing-box/release/DEFAULT_BUILD_TAGS_OTHERS)
          fi
          if [[ "${{ matrix.naive }}" == "true" && "${TAGS}" != *with_naive_outbound* ]]; then
            TAGS="${TAGS},with_naive_outbound"
          fi
          if [[ -d sing-box/common/ebpf && ("${{ matrix.platform }}" == "linux" || "${{ matrix.platform }}" == "android") && "${TAGS}" != *with_ebpf* ]]; then
            TAGS="${TAGS},with_ebpf"
          fi
          ARCHIVE_SUFFIX=""

          case "${{ matrix.variant }}" in
            'purego')
              if [[ "${TAGS}" != *with_purego* ]]; then
                TAGS="${TAGS},with_purego"
              fi
              ;;
            'glibc')
              ARCHIVE_SUFFIX="-glibc"
              ;;
            'musl')
              TAGS="${TAGS},with_musl"
              ARCHIVE_SUFFIX="-musl"
              ;;
          esac
          if [[ "${{ matrix.variant }}" == "purego" && "${{ matrix.platform }}" == "linux" ]]; then
              ARCHIVE_SUFFIX="-purego"
          fi

          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
          echo "ldflags=$(cat sing-box/release/LDFLAGS)" >> "$GITHUB_OUTPUT"
          echo "archive_suffix=${ARCHIVE_SUFFIX}" >> "$GITHUB_OUTPUT"

          echo "--- Build Environment ---"
          echo "Platform: ${{ matrix.platform }}"
          echo "Arch: ${{ matrix.arch }}"
          echo "Tags: ${TAGS}"
          echo "Archive suffix: ${ARCHIVE_SUFFIX}"

      - name: Build
        shell: bash
        run: |
          set -xeuo pipefail
          cd sing-box
          mkdir -p dist
          if [[ -n "${{ matrix.cc_triple }}" ]]; then
            export CC="${{ steps.setup-ndk.outputs.ndk-path }}/toolchains/llvm/prebuilt/linux-x86_64/bin/${{ matrix.cc_triple }}-clang"
          fi
          go build -v -trimpath -o "dist/${{ matrix.binary }}" \
            -tags "${{ steps.env.outputs.tags }}" \
            -ldflags "-X github.com/sagernet/sing-box/constant.Version=${{ needs.resolve_source.outputs.core_version }} ${LDFLAGS_SHARED} -s -w -buildid=" \
            ./cmd/sing-box
        env:
          LDFLAGS_SHARED: ${{ steps.env.outputs.ldflags }}
          CGO_ENABLED: ${{ matrix.cgo || '0' }}
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          GOAMD64: ${{ matrix.goamd64 }}
          MACOSX_DEPLOYMENT_TARGET: ${{ matrix.macos_deployment_target }}

      - name: Extract libcronet.so
        if: matrix.variant == 'purego' && matrix.platform == 'linux'
        shell: bash
        run: |
          set -xeuo pipefail
          CRONET_GO_VERSION=$(cat sing-box/.github/CRONET_GO_VERSION)
          LIBRARY_PATH="lib/linux_${{ matrix.goarch }}/libcronet.so"
          git init "$RUNNER_TEMP/cronet-go-lib"
          git -C "$RUNNER_TEMP/cronet-go-lib" remote add origin https://github.com/sagernet/cronet-go.git
          git -C "$RUNNER_TEMP/cronet-go-lib" sparse-checkout set --no-cone "/$LIBRARY_PATH"
          git -C "$RUNNER_TEMP/cronet-go-lib" fetch --depth=1 --filter=blob:none origin "$CRONET_GO_VERSION"
          git -C "$RUNNER_TEMP/cronet-go-lib" checkout FETCH_HEAD
          cp "$RUNNER_TEMP/cronet-go-lib/$LIBRARY_PATH" sing-box/dist/

      - name: Extract libcronet.dll
        if: matrix.variant == 'purego' && matrix.platform == 'windows'
        shell: pwsh
        run: |
          $CRONET_GO_VERSION = Get-Content sing-box/.github/CRONET_GO_VERSION
          $LIBRARY_PATH = "lib/windows_${{ matrix.goarch }}/libcronet.dll"
          git init "$env:RUNNER_TEMP/cronet-go-lib"
          git -C "$env:RUNNER_TEMP/cronet-go-lib" remote add origin https://github.com/sagernet/cronet-go.git
          git -C "$env:RUNNER_TEMP/cronet-go-lib" sparse-checkout set --no-cone "/$LIBRARY_PATH"
          git -C "$env:RUNNER_TEMP/cronet-go-lib" fetch --depth=1 --filter=blob:none origin "$CRONET_GO_VERSION"
          git -C "$env:RUNNER_TEMP/cronet-go-lib" checkout FETCH_HEAD
          Copy-Item "$env:RUNNER_TEMP/cronet-go-lib/$LIBRARY_PATH" sing-box/dist

      - name: Archive (Linux)
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          cd sing-box/dist
          DIR_NAME="sing-box-${{ needs.resolve_source.outputs.core_version }}-linux-${{ matrix.arch }}${{ steps.env.outputs.archive_suffix }}"
          mkdir -p "$DIR_NAME"
          cp ../LICENSE "$DIR_NAME/"
          cp sing-box "$DIR_NAME/"
          if [ -f libcronet.so ]; then
              cp libcronet.so "${DIR_NAME}"
          fi
          tar -czvf "$DIR_NAME.tar.gz" "$DIR_NAME"
          rm -rf "$DIR_NAME" sing-box
          if [ -f libcronet.so ]; then rm -f libcronet.so; fi

      - name: Archive (Windows)
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          cd sing-box/dist
          $DIR_NAME = "sing-box-${{ needs.resolve_source.outputs.core_version }}-windows-${{ matrix.arch }}"
          mkdir "$DIR_NAME"
          Copy-Item ../LICENSE "$DIR_NAME/"
          Copy-Item sing-box.exe "$DIR_NAME/"
          if (Test-Path libcronet.dll) {
              Copy-Item libcronet.dll "$DIR_NAME/"
          }
          Compress-Archive -Path "$DIR_NAME" -DestinationPath "$DIR_NAME.zip"
          Remove-Item -Recurse "$DIR_NAME"
          Remove-Item sing-box.exe
          if (Test-Path libcronet.dll) { Remove-Item libcronet.dll }

      - name: Archive (Darwin)
        if: matrix.platform == 'darwin'
        run: |
          cd sing-box/dist
          DIR_NAME="sing-box-${{ needs.resolve_source.outputs.core_version }}-darwin-${{ matrix.arch }}"
          if [[ -n "${{ matrix.legacy_name }}" ]]; then
            DIR_NAME="${DIR_NAME}-legacy-${{ matrix.legacy_name }}"
          fi
          mkdir -p "$DIR_NAME"
          cp ../LICENSE "$DIR_NAME/"
          cp sing-box "$DIR_NAME/"
          tar -czvf "$DIR_NAME.tar.gz" "$DIR_NAME"
          rm -rf "$DIR_NAME" sing-box

      - name: Archive (Android)
        if: matrix.platform == 'android'
        run: |
          cd sing-box/dist
          DIR_NAME="sing-box-${{ needs.resolve_source.outputs.core_version }}-android-${{ matrix.arch }}"
          mkdir -p "$DIR_NAME"
          cp ../LICENSE "$DIR_NAME/"
          cp sing-box "$DIR_NAME/"
          tar -czvf "$DIR_NAME.tar.gz" "$DIR_NAME"
          rm -rf "$DIR_NAME" sing-box

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: sing-box/dist/*

  build_android_library:
    if: ${{ github.event.inputs.build_android_gui == 'true' }}
    name: Build Android library (${{ matrix.go_arch }})
    runs-on: ubuntu-latest
    needs: resolve_source
    strategy:
      fail-fast: false
      matrix:
        go_arch: ['386', amd64, arm, arm64]
    steps:
      - name: Checkout sing-box
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.core_repository }}
          ref: ${{ needs.resolve_source.outputs.source_sha }}
          path: sing-box
          fetch-depth: 1

      - name: Set build tag
        run: git -C sing-box tag -f "v${{ needs.resolve_source.outputs.core_version }}" "${{ needs.resolve_source.outputs.source_sha }}"

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Cache Go modules
        uses: actions/cache@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-linux-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-mod-linux-

      - name: Setup Android NDK
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r28

      - name: Setup OpenJDK
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: 17

      - name: Build library
        run: |
          set -euo pipefail
          cd sing-box
          make lib_install
          export PATH="$PATH:$(go env GOPATH)/bin"
          go run ./cmd/internal/build_libbox -target android -platform android/${{ matrix.go_arch }}
          mkdir -p "dist/${{ matrix.go_arch }}"
          mv libbox.aar libbox-legacy.aar "dist/${{ matrix.go_arch }}"
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}

      - name: Upload library
        uses: actions/upload-artifact@v7
        with:
          name: android-library-${{ matrix.go_arch }}
          path: sing-box/dist
          compression-level: 0

  build_android:
    if: ${{ github.event.inputs.build_android_gui == 'true' }}
    name: Build Android (${{ matrix.variant }})
    runs-on: ubuntu-latest
    needs: [resolve_source, build_android_library]
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: other
            task: :app:assembleOtherRelease
            output: app/build/outputs/apk/other/release/*.apk
          - variant: other-legacy
            task: :app:assembleOtherLegacyRelease
            output: app/build/outputs/apk/otherLegacy/release/*.apk
    steps:
      - name: Checkout sing-box
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.core_repository }}
          ref: ${{ needs.resolve_source.outputs.source_sha }}
          path: sing-box
          fetch-depth: 1

      - name: Set build tag
        run: git -C sing-box tag -f "v${{ needs.resolve_source.outputs.core_version }}" "${{ needs.resolve_source.outputs.source_sha }}"

      - name: Checkout sing-box-for-android
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.android_gui_repository }}
          ref: ${{ needs.resolve_source.outputs.android_gui_sha }}
          path: sing-box-for-android

      - name: Checkout submodule recursive
        run: git -C sing-box-for-android submodule update --init --recursive -v

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Restore Go modules
        uses: actions/cache/restore@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-linux-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-mod-linux-

      - name: Cache Go tool builds
        uses: actions/cache@v5
        with:
          path: ~/.cache/go-build
          key: go-build-tools-linux-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-build-tools-linux-

      - name: Setup Android NDK
        id: setup-ndk
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r28

      - name: Setup OpenJDK
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # v6.2.0
        with:
          cache-provider: basic

      - name: Update Android application version
        run: |
          set -euo pipefail
          file="sing-box-for-android/version.properties"
          sed -i "s/^VERSION_NAME=.*/VERSION_NAME=${{ github.event.inputs.version }}/" "$file"
          grep '^VERSION_NAME=' "$file"

      - name: Checkout release CI scripts
        uses: actions/checkout@v6
        with:
          path: release-ci
          fetch-depth: 1

      - name: Rewrite Android package identifiers
        run: bash release-ci/.github/scripts/rewrite-gui-package-ids.sh android sing-box-for-android "${{ github.repository }}"

      - name: Read version code from source
        run: |
          version_code=$(grep '^VERSION_CODE=' sing-box-for-android/version.properties | cut -d'=' -f2)
          echo "VERSION_CODE=${version_code}" >> "$GITHUB_ENV"

      - name: Download libraries
        uses: actions/download-artifact@v8
        with:
          pattern: android-library-*
          path: libbox-inputs
          merge-multiple: true

      - name: Merge libraries
        run: |
          set -euo pipefail
          cd sing-box
          go run ./cmd/internal/merge_aar -output ../libbox.aar ../libbox-inputs/*/libbox.aar
          go run ./cmd/internal/merge_aar -output ../libbox-legacy.aar ../libbox-inputs/*/libbox-legacy.aar

      - name: Prepare Android client
        run: |
          mkdir -p sing-box-for-android/app/libs
          cp libbox.aar libbox-legacy.aar sing-box-for-android/app/libs/

      - name: Decode Keystore
        run: echo "${{ secrets.RELEASE_KEYSTORE_FILE }}" | base64 -d > sing-box-for-android/app/release.keystore

      - name: Build APK
        run: |
          cd sing-box-for-android
          chmod +x gradlew
          ./gradlew --build-cache --parallel ${{ matrix.task }} \
            -Pandroid.injected.signing.store.file=$(pwd)/app/release.keystore \
            -Pandroid.injected.signing.store.password="${{ secrets.RELEASE_KEYSTORE_PASSWORD }}" \
            -Pandroid.injected.signing.key.alias="${{ secrets.RELEASE_KEY_ALIAS }}" \
            -Pandroid.injected.signing.key.password="${{ secrets.RELEASE_KEY_PASSWORD }}"
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}

      - name: Prepare upload
        run: |
          set -euo pipefail
          mkdir -p dist
          cp sing-box-for-android/${{ matrix.output }} dist
          if [[ "${{ matrix.variant }}" == "other" ]]; then
            printf '{\n    "version_code": %s,\n    "version_name": "%s"\n}\n' \
              "$VERSION_CODE" "${{ github.event.inputs.version }}" > dist/SFA-version-metadata.json
            cat dist/SFA-version-metadata.json
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: android-apk-${{ matrix.variant }}
          path: dist

  build_windows_gui:
    if: ${{ github.event.inputs.build_windows_gui == 'true' }}
    name: Build Windows GUI (${{ matrix.desktop_arch }})
    runs-on: windows-latest
    needs: resolve_source
    strategy:
      fail-fast: false
      matrix:
        include:
          - desktop_arch: x64
            rust_target: x86_64-pc-windows-msvc
          - desktop_arch: x86
            rust_target: i686-pc-windows-msvc
          - desktop_arch: arm64
            rust_target: aarch64-pc-windows-msvc
    steps:
      - name: Checkout sing-box
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.core_repository }}
          ref: ${{ needs.resolve_source.outputs.source_sha }}
          path: sing-box
          fetch-depth: 1

      - name: Checkout sing-box-for-desktop
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.desktop_gui_repository }}
          ref: ${{ needs.resolve_source.outputs.desktop_gui_sha }}
          path: sing-box-for-desktop
          submodules: recursive

      - name: Set build tag
        shell: pwsh
        run: |
          $tag = "v${{ needs.resolve_source.outputs.core_version }}"
          git -C sing-box tag -f $tag "${{ needs.resolve_source.outputs.source_sha }}"

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Cache Go modules
        uses: actions/cache@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-desktop-windows-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-mod-desktop-windows-

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@1.88.0
        with:
          targets: ${{ matrix.rust_target }}

      - name: Cache Windows sharing module
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/git
            ~/.cargo/registry
            sing-box-for-desktop/bin/windows-share-toolchain/cargo-target
          key: windows-share-${{ matrix.rust_target }}-${{ hashFiles('sing-box-for-desktop/native/windows-share/Cargo.lock') }}
          restore-keys: windows-share-${{ matrix.rust_target }}-

      - name: Setup pnpm
        uses: pnpm/action-setup@v4.4.0
        with:
          package_json_file: sing-box-for-desktop/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: sing-box-for-desktop/package.json
          cache: pnpm
          cache-dependency-path: |
            sing-box-for-desktop/pnpm-lock.yaml
            sing-box-for-desktop/dashboard/pnpm-lock.yaml

      - name: Update version
        shell: pwsh
        run: |
          $versionPath = 'sing-box-for-desktop/version.json'
          $versionMetadata = Get-Content -Raw $versionPath | ConvertFrom-Json
          $versionMetadata.version = $env:DESKTOP_VERSION
          $versionMetadata | ConvertTo-Json -Depth 100 | Set-Content -Encoding utf8NoBOM $versionPath
          Get-Content $versionPath
        env:
          DESKTOP_VERSION: ${{ github.event.inputs.version }}

      - name: Checkout release CI scripts
        uses: actions/checkout@v6
        with:
          path: release-ci
          fetch-depth: 1

      - name: Rewrite desktop and core package identifiers
        shell: bash
        run: |
          set -euo pipefail
          bash release-ci/.github/scripts/rewrite-gui-package-ids.sh desktop sing-box-for-desktop "${{ github.repository }}"
          bash release-ci/.github/scripts/rewrite-gui-package-ids.sh core sing-box "${{ github.repository }}"

      - name: Setup signing
        shell: pwsh
        run: |
          if ([string]::IsNullOrWhiteSpace($env:WINDOWS_CERTIFICATES_P12)) {
            throw 'WINDOWS_CERTIFICATES_P12 is not configured'
          }
          if ([string]::IsNullOrWhiteSpace($env:WINDOWS_P12_PASSWORD)) {
            throw 'WINDOWS_P12_PASSWORD is not configured'
          }
          $certificatePath = Join-Path $env:RUNNER_TEMP 'windows-signing.p12'
          [IO.File]::WriteAllBytes($certificatePath, [Convert]::FromBase64String($env:WINDOWS_CERTIFICATES_P12))
          $certificate = [System.Security.Cryptography.X509Certificates.X509Certificate2]::new(
            $certificatePath,
            $env:WINDOWS_P12_PASSWORD,
            [System.Security.Cryptography.X509Certificates.X509KeyStorageFlags]::EphemeralKeySet
          )
          if (-not $certificate.HasPrivateKey) {
            throw 'The Windows signing certificate does not contain a private key'
          }
          $codeSigningOid = '1.3.6.1.5.5.7.3.3'
          $enhancedKeyUsage = @($certificate.Extensions |
            Where-Object { $_ -is [System.Security.Cryptography.X509Certificates.X509EnhancedKeyUsageExtension] })
          if ($enhancedKeyUsage.Count -eq 0 -or
              -not ($enhancedKeyUsage.EnhancedKeyUsages.Value -contains $codeSigningOid)) {
            throw 'The Windows certificate is not valid for code signing'
          }
          if ($certificate.NotAfter -le (Get-Date)) {
            throw "The Windows signing certificate expired at $($certificate.NotAfter.ToString('u'))"
          }
          $publicCertificatePath = Join-Path $env:RUNNER_TEMP "windows-signing-$($certificate.Thumbprint).cer"
          [IO.File]::WriteAllBytes(
            $publicCertificatePath,
            $certificate.Export([System.Security.Cryptography.X509Certificates.X509ContentType]::Cert)
          )
          "WINDOWS_SIGNING_PUBLIC_CERTIFICATE=$publicCertificatePath" |
            Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          @{
            windows = @{
              certificateFile = $certificatePath
              certificatePassword = $env:WINDOWS_P12_PASSWORD
            }
          } | ConvertTo-Json -Depth 3 | Set-Content -Encoding utf8NoBOM sing-box-for-desktop/signing.local.json
        env:
          WINDOWS_CERTIFICATES_P12: ${{ secrets.WINDOWS_CERTIFICATES_P12 }}
          WINDOWS_P12_PASSWORD: ${{ secrets.WINDOWS_P12_PASSWORD }}

      - name: Install dependencies
        run: pnpm -C sing-box-for-desktop install

      - name: Build
        run: pnpm -C sing-box-for-desktop package:win -- ${{ matrix.desktop_arch }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare upload
        shell: pwsh
        run: |
          $artifacts = @(Get-ChildItem sing-box-for-desktop/release/SFW-*.exe -File)
          if ($artifacts.Count -ne 1) {
            throw "expected 1 Windows installer for ${{ matrix.desktop_arch }}, found $($artifacts.Count)"
          }
          New-Item -ItemType Directory -Force dist | Out-Null
          Copy-Item $artifacts.FullName dist
          if ('${{ matrix.desktop_arch }}' -eq 'x64') {
            Copy-Item $env:WINDOWS_SIGNING_PUBLIC_CERTIFICATE dist
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: windows-gui-${{ matrix.desktop_arch }}
          path: dist

  build_linux_gui:
    if: ${{ github.event.inputs.build_linux_gui == 'true' }}
    name: Build Linux GUI ${{ matrix.desktop_arch }}
    runs-on: ubuntu-26.04
    needs: resolve_source
    strategy:
      fail-fast: false
      matrix:
        include:
          - desktop_arch: x64
            go_arch: amd64
            go_arm: ''
          - desktop_arch: arm64
            go_arch: arm64
            go_arm: ''
          - desktop_arch: armv7l
            go_arch: arm
            go_arm: '7'
    steps:
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Checkout sing-box
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.core_repository }}
          ref: ${{ needs.resolve_source.outputs.source_sha }}
          path: sing-box
          fetch-depth: 1

      - name: Checkout sing-box-for-desktop
        uses: actions/checkout@v6
        with:
          repository: ${{ needs.resolve_source.outputs.desktop_gui_repository }}
          ref: ${{ needs.resolve_source.outputs.desktop_gui_sha }}
          path: sing-box-for-desktop
          submodules: recursive

      - name: Cache Go modules
        uses: actions/cache@v5
        with:
          path: ~/go/pkg/mod
          key: go-mod-desktop-linux-${{ hashFiles('sing-box/go.sum') }}
          restore-keys: go-mod-desktop-linux-

      - name: Set build tag
        run: |
          set -xeuo pipefail
          cd sing-box
          TAG="v${{ needs.resolve_source.outputs.core_version }}"
          git tag -f "${TAG}" "${{ needs.resolve_source.outputs.source_sha }}"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4.4.0
        with:
          package_json_file: sing-box-for-desktop/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: sing-box-for-desktop/package.json
          cache: pnpm
          cache-dependency-path: |
            sing-box-for-desktop/pnpm-lock.yaml
            sing-box-for-desktop/dashboard/pnpm-lock.yaml

      - name: Update version
        run: |
          jq --arg version "$DESKTOP_VERSION" \
            '.version = $version' \
            sing-box-for-desktop/version.json > "$RUNNER_TEMP/desktop-version.json"
          mv "$RUNNER_TEMP/desktop-version.json" sing-box-for-desktop/version.json
          cat sing-box-for-desktop/version.json
        env:
          DESKTOP_VERSION: ${{ github.event.inputs.version }}

      - name: Checkout release CI scripts
        uses: actions/checkout@v6
        with:
          path: release-ci
          fetch-depth: 1

      - name: Rewrite desktop and core package identifiers
        run: |
          set -euo pipefail
          bash release-ci/.github/scripts/rewrite-gui-package-ids.sh desktop sing-box-for-desktop "${{ github.repository }}"
          bash release-ci/.github/scripts/rewrite-gui-package-ids.sh core sing-box "${{ github.repository }}"

      - name: Clone cronet-go
        run: |
          set -xeuo pipefail
          CRONET_GO_VERSION=$(cat sing-box/.github/CRONET_GO_VERSION)
          git init ~/cronet-go
          git -C ~/cronet-go remote add origin https://github.com/sagernet/cronet-go.git
          git -C ~/cronet-go fetch --depth=1 origin "$CRONET_GO_VERSION"
          git -C ~/cronet-go checkout FETCH_HEAD
          git -C ~/cronet-go submodule update --init --recursive --depth=1

      - name: Regenerate Debian keyring
        run: |
          set -xeuo pipefail
          rm -f ~/cronet-go/naiveproxy/src/build/linux/sysroot_scripts/keyring.gpg
          cd ~/cronet-go
          GPG_TTY=/dev/null ./naiveproxy/src/build/linux/sysroot_scripts/generate_keyring.sh

      - name: Download Chromium toolchain
        run: |
          set -xeuo pipefail
          cd ~/cronet-go
          go run ./cmd/build-naive --target=linux/${{ matrix.go_arch }} download-toolchain

      - name: Set Chromium toolchain environment
        run: |
          set -xeuo pipefail
          cd ~/cronet-go
          go run ./cmd/build-naive --target=linux/${{ matrix.go_arch }} env >> "$GITHUB_ENV"

      - name: Install package tools
        run: |
          set -euo pipefail
          sudo apt-get update || true
          sudo apt-get install -y debsigs debsig-verify libarchive-tools rpm zstd
          echo '%_rpmformat 4' > "$HOME/.rpmmacros"

      - name: Install dependencies
        run: pnpm -C sing-box-for-desktop install

      - name: Build
        run: pnpm -C sing-box-for-desktop package:linux -- deb rpm pacman ${{ matrix.desktop_arch }}
        env:
          CGO_ENABLED: '0'
          GOARM: ${{ matrix.go_arm }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup package signing
        run: |
          set -euo pipefail
          [[ -n "$GPG_KEY" ]] || { echo 'GPG_KEY is not configured' >&2; exit 1; }
          [[ -n "$GPG_KEY_ID" ]] || { echo 'GPG_KEY_ID is not configured' >&2; exit 1; }
          [[ -n "$GPG_PASSPHRASE" ]] || { echo 'GPG_PASSPHRASE is not configured' >&2; exit 1; }

          SIGNING_GNUPG_HOME="$RUNNER_TEMP/package-signing-gnupg"
          SIGNING_PASSPHRASE_FILE="$RUNNER_TEMP/package-signing-passphrase"
          mkdir -m 700 "$SIGNING_GNUPG_HOME"
          printf '%s' "$GPG_PASSPHRASE" > "$SIGNING_PASSPHRASE_FILE"
          chmod 600 "$SIGNING_PASSPHRASE_FILE"
          {
            echo "GNUPGHOME=$SIGNING_GNUPG_HOME"
            echo "SIGNING_PASSPHRASE_FILE=$SIGNING_PASSPHRASE_FILE"
          } >> "$GITHUB_ENV"

          printf '%s' "$GPG_KEY" | gpg \
            --homedir "$SIGNING_GNUPG_HOME" \
            --batch \
            --pinentry-mode loopback \
            --passphrase-file "$SIGNING_PASSPHRASE_FILE" \
            --import
          gpg --homedir "$SIGNING_GNUPG_HOME" \
            --batch \
            --list-secret-keys "$GPG_KEY_ID" >/dev/null
          SIGNING_FINGERPRINT=$(gpg \
            --homedir "$SIGNING_GNUPG_HOME" \
            --batch \
            --with-colons \
            --list-secret-keys "$GPG_KEY_ID" |
            awk -F: '$1 == "fpr" { print $10; exit }')
          [[ "$SIGNING_FINGERPRINT" =~ ^[0-9A-Fa-f]{40}$ ]] || {
            echo "failed to resolve a 40-character GPG fingerprint" >&2
            exit 1
          }
          SIGNING_LONG_KEY_ID="${SIGNING_FINGERPRINT: -16}"
          SIGNING_PUBLIC_KEY="$RUNNER_TEMP/GPG-${SIGNING_FINGERPRINT}.asc"
          DEBSIGS_GPG_DIR="$RUNNER_TEMP/debsigs-gpg"
          REAL_GPG=$(command -v gpg)
          mkdir -m 700 "$DEBSIGS_GPG_DIR"
          cat > "$DEBSIGS_GPG_DIR/gpg" <<EOF
          #!/bin/sh
          exec "$REAL_GPG" --batch --pinentry-mode loopback --passphrase-file "$SIGNING_PASSPHRASE_FILE" "\$@"
          EOF
          chmod 700 "$DEBSIGS_GPG_DIR/gpg"
          gpg --homedir "$SIGNING_GNUPG_HOME" \
            --batch \
            --armor \
            --export "$SIGNING_FINGERPRINT" > "$SIGNING_PUBLIC_KEY"
          {
            echo "SIGNING_FINGERPRINT=$SIGNING_FINGERPRINT"
            echo "SIGNING_LONG_KEY_ID=$SIGNING_LONG_KEY_ID"
            echo "SIGNING_PUBLIC_KEY=$SIGNING_PUBLIC_KEY"
            echo "DEBSIGS_GPG_DIR=$DEBSIGS_GPG_DIR"
          } >> "$GITHUB_ENV"

          cat > "$HOME/.rpmmacros" <<EOF
          %_gpg_name $GPG_KEY_ID
          %_gpg_sign_cmd_extra_args --pinentry-mode loopback --passphrase-file $SIGNING_PASSPHRASE_FILE
          EOF
        env:
          GPG_KEY: ${{ secrets.GPG_KEY }}
          GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}

      - name: Sign Linux packages
        run: |
          set -euo pipefail
          shopt -s nullglob
          deb_packages=(sing-box-for-desktop/release/SFL-*.deb)
          rpm_packages=(sing-box-for-desktop/release/SFL-*.rpm)
          pacman_packages=(sing-box-for-desktop/release/SFL-*.pkg.tar.zst)
          if (( ${#deb_packages[@]} != 1 || ${#rpm_packages[@]} != 1 || ${#pacman_packages[@]} != 1 )); then
            echo "expected exactly one deb, rpm and pacman package" >&2
            exit 1
          fi
          PATH="$DEBSIGS_GPG_DIR:$PATH" debsigs \
            --sign=origin \
            --default-key="$GPG_KEY_ID" \
            "${deb_packages[0]}"
          rpmsign --addsign "${rpm_packages[0]}"
          gpg \
            --batch \
            --yes \
            --pinentry-mode loopback \
            --passphrase-file "$SIGNING_PASSPHRASE_FILE" \
            --local-user "$GPG_KEY_ID" \
            --detach-sign \
            --output "${pacman_packages[0]}.sig" \
            "${pacman_packages[0]}"
        env:
          GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}

      - name: Verify Linux package signatures
        run: |
          set -euo pipefail
          shopt -s nullglob
          deb_packages=(sing-box-for-desktop/release/SFL-*.deb)
          rpm_packages=(sing-box-for-desktop/release/SFL-*.rpm)
          pacman_packages=(sing-box-for-desktop/release/SFL-*.pkg.tar.zst)

          DEBSIG_ROOT="$RUNNER_TEMP/debsig-root"
          policy_dir="$DEBSIG_ROOT/etc/debsig/policies/$SIGNING_LONG_KEY_ID"
          keyring_dir="$DEBSIG_ROOT/usr/share/debsig/keyrings/$SIGNING_LONG_KEY_ID"
          mkdir -p "$policy_dir" "$keyring_dir"
          gpg --batch --export "$SIGNING_FINGERPRINT" > "$keyring_dir/signing-key.pgp"
          cat > "$policy_dir/signing-key.pol" <<EOF
          <?xml version="1.0"?>
          <!DOCTYPE Policy SYSTEM "https://www.debian.org/debsig/1.0/policy.dtd">
          <Policy xmlns="https://www.debian.org/debsig/1.0/">
            <Origin Name="sing-box releases" id="$SIGNING_LONG_KEY_ID" Description="sing-box package signing key"/>
            <Selection>
              <Required Type="origin" File="signing-key.pgp" id="$SIGNING_LONG_KEY_ID"/>
            </Selection>
            <Verification MinOptional="0">
              <Required Type="origin" File="signing-key.pgp" id="$SIGNING_LONG_KEY_ID"/>
            </Verification>
          </Policy>
          EOF
          debsig-verify --root "$DEBSIG_ROOT" "${deb_packages[0]}"

          sudo rpm --import "$SIGNING_PUBLIC_KEY"
          rpm --checksig "${rpm_packages[0]}"

          gpg --batch --verify "${pacman_packages[0]}.sig" "${pacman_packages[0]}"

      - name: Prepare upload
        run: |
          set -euo pipefail
          shopt -s nullglob
          artifacts=(
            sing-box-for-desktop/release/SFL-*.deb
            sing-box-for-desktop/release/SFL-*.rpm
            sing-box-for-desktop/release/SFL-*.pkg.tar.zst
            sing-box-for-desktop/release/SFL-*.pkg.tar.zst.sig
          )
          if (( ${#artifacts[@]} != 4 )); then
            echo "expected 4 signed Linux package files, found ${#artifacts[@]}" >&2
            exit 1
          fi
          mkdir -p dist
          cp "${artifacts[@]}" dist
          if [[ "${{ matrix.desktop_arch }}" == "x64" ]]; then
            cp "$SIGNING_PUBLIC_KEY" dist/
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: linux-gui-${{ matrix.desktop_arch }}
          path: dist

  finalize_build:
    name: Finalize build artifacts
    runs-on: ubuntu-latest
    needs: [resolve_source, build_cli, build_android_library, build_android, build_windows_gui, build_linux_gui]
    if: ${{ always() && needs.resolve_source.result == 'success' && (inputs.build_cli || inputs.build_android_gui || inputs.build_windows_gui || inputs.build_linux_gui) && (!inputs.build_cli || needs.build_cli.result == 'success') && (!inputs.build_android_gui || (needs.build_android_library.result == 'success' && needs.build_android.result == 'success')) && (!inputs.build_windows_gui || needs.build_windows_gui.result == 'success') && (!inputs.build_linux_gui || needs.build_linux_gui.result == 'success') }}
    permissions:
      actions: read
      contents: read
      id-token: write
      attestations: write
      artifact-metadata: write
    steps:
      - name: Download CLI artifacts
        if: inputs.build_cli
        uses: actions/download-artifact@v8
        with:
          pattern: binary-*
          path: artifacts
          merge-multiple: true

      - name: Download Android artifacts
        if: inputs.build_android_gui
        uses: actions/download-artifact@v8
        with:
          pattern: android-apk-*
          path: artifacts
          merge-multiple: true

      - name: Download Windows GUI artifacts
        if: inputs.build_windows_gui
        uses: actions/download-artifact@v8
        with:
          pattern: windows-gui-*
          path: artifacts
          merge-multiple: true

      - name: Download Linux GUI artifacts
        if: inputs.build_linux_gui
        uses: actions/download-artifact@v8
        with:
          pattern: linux-gui-*
          path: artifacts
          merge-multiple: true

      - name: Generate build manifest
        run: |
          set -euo pipefail
          {
            while IFS= read -r -d '' file; do
              relative_path=${file#artifacts/}
              sha256=$(sha256sum "$file" | awk '{print $1}')
              size=$(stat -c '%s' "$file")
              jq -nc \
                --arg path "$relative_path" \
                --arg sha256 "$sha256" \
                --argjson size "$size" \
                '{path: $path, sha256: $sha256, size: $size}'
            done < <(find artifacts -type f -print0 | sort -z)
          } | jq -s 'sort_by(.path)' > artifact-files.json

          jq -n \
            --arg workflow '.github/workflows/build-testing.yml' \
            --arg repository "${{ github.repository }}" \
            --arg head_branch "${{ github.ref_name }}" \
            --arg head_sha "${{ github.sha }}" \
            --arg version "${{ inputs.version }}" \
            --arg core_base_version "${{ inputs.core_base_version }}" \
            --arg core_version "${{ needs.resolve_source.outputs.core_version }}" \
            --arg core_repository "${{ needs.resolve_source.outputs.core_repository }}" \
            --arg core_ref "${{ needs.resolve_source.outputs.core_ref }}" \
            --arg source_sha "${{ needs.resolve_source.outputs.source_sha }}" \
            --arg android_gui_repository "${{ needs.resolve_source.outputs.android_gui_repository }}" \
            --arg android_gui_ref "${{ needs.resolve_source.outputs.android_gui_ref }}" \
            --arg android_gui_sha "${{ needs.resolve_source.outputs.android_gui_sha }}" \
            --arg desktop_gui_repository "${{ needs.resolve_source.outputs.desktop_gui_repository }}" \
            --arg desktop_gui_ref "${{ needs.resolve_source.outputs.desktop_gui_ref }}" \
            --arg desktop_gui_sha "${{ needs.resolve_source.outputs.desktop_gui_sha }}" \
            --arg built_at "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            --argjson run_id "${{ github.run_id }}" \
            --argjson run_attempt "${{ github.run_attempt }}" \
            --argjson build_cli "${{ inputs.build_cli }}" \
            --argjson build_android_gui "${{ inputs.build_android_gui }}" \
            --argjson build_windows_gui "${{ inputs.build_windows_gui }}" \
            --argjson build_linux_gui "${{ inputs.build_linux_gui }}" \
            --slurpfile artifact_files artifact-files.json \
            '{
              schema_version: 1,
              workflow: $workflow,
              repository: $repository,
              head_branch: $head_branch,
              head_sha: $head_sha,
              run_id: $run_id,
              run_attempt: $run_attempt,
              version: $version,
              core_base_version: $core_base_version,
              core_version: $core_version,
              source_repository: $core_repository,
              source_ref: $core_ref,
              source_sha: $source_sha,
              sources: {
                core: {
                  repository: $core_repository,
                  ref: $core_ref,
                  sha: $source_sha
                },
                android_gui: {
                  repository: $android_gui_repository,
                  ref: $android_gui_ref,
                  sha: $android_gui_sha
                },
                desktop_gui: {
                  repository: $desktop_gui_repository,
                  ref: $desktop_gui_ref,
                  sha: $desktop_gui_sha
                }
              },
              built_at: $built_at,
              build: {
                cli: $build_cli,
                android_gui: $build_android_gui,
                windows_gui: $build_windows_gui,
                linux_gui: $build_linux_gui
              },
              artifacts: $artifact_files[0]
            }' > build-manifest.json

          jq -e '.artifacts | length > 0' build-manifest.json >/dev/null
          cat build-manifest.json

      - name: Attest build provenance
        uses: actions/attest@f7c74d28b9d84cb8768d0b8ca14a4bac6ef463e6 # v4.2.0
        with:
          subject-path: |
            artifacts/**
            build-manifest.json

      - name: Upload build manifest
        uses: actions/upload-artifact@v7
        with:
          name: build-manifest
          path: build-manifest.json

  cache_gc:
    name: Cache GC
    if: always()
    runs-on: ubuntu-latest
    needs: finalize_build
    permissions:
      actions: write
    steps:
      - name: Delete superseded caches
        env:
          GH_TOKEN: ${{ github.token }}
        run: |-
          set -euo pipefail
          gh api "repos/$GITHUB_REPOSITORY/actions/caches?per_page=100" --paginate \
            --jq '.actions_caches[] | [.id, .ref, .created_at, .last_accessed_at, .size_in_bytes, .key] | @tsv' > "$RUNNER_TEMP/caches.tsv"
          python3 - "$GITHUB_REPOSITORY" "$RUNNER_TEMP/caches.tsv" <<'EOF'
          import re
          import subprocess
          import sys

          repository, list_path = sys.argv[1], sys.argv[2]
          budget = 9 * 1024 * 1024 * 1024
          rows = []
          with open(list_path) as list_file:
              for line in list_file:
                  cache_id, ref, created_at, accessed_at, size, key = line.rstrip("\n").split("\t")
                  family = key
                  while True:
                      stripped = re.sub(r"-[0-9a-f]{16,}$", "", family)
                      if stripped == family:
                          break
                      family = stripped
                  rows.append({
                      "id": cache_id, "ref": ref, "created": created_at,
                      "accessed": accessed_at, "size": int(size), "key": key,
                      "family": family,
                  })
          newest = {}
          for row in rows:
              group = (row["ref"], row["family"])
              if group not in newest or row["created"] > newest[group]["created"]:
                  newest[group] = row
          doomed = [row for row in rows if newest[(row["ref"], row["family"])] is not row]
          kept = [row for row in rows if newest[(row["ref"], row["family"])] is row]
          total = sum(row["size"] for row in kept)
          for row in sorted(kept, key=lambda entry: entry["accessed"]):
              if total <= budget:
                  break
              doomed.append(row)
              total -= row["size"]
          for row in doomed:
              print(f"delete {row['size'] >> 20}MB {row['key']} ({row['ref']})")
              subprocess.run(
                  ["gh", "api", "-X", "DELETE", f"repos/{repository}/actions/caches/{row['id']}"],
                  check=False,
              )
          print(f"kept {len(rows) - len(doomed)} caches, {total >> 20}MB total")
          EOF
